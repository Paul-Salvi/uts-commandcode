# workflow
- When a batch of work spans multiple repos (backend, frontend, root with submodules) and the user asks to "check git status... give me commit msg for all", run `git status`/`git diff --stat` in every repo and present the changes as a per-repo summary broken into logical units, with one suggested commit message per unit (not a single blanket message) and a concrete commit plan (commit each repo separately, update root submodule pointers last). The user expects a ready-to-use change summary plus commit messages as part of the delivery loop. When the user then says "commit it separately", execute exactly that plan: stage files by logical concern per repo, commit each unit in order, verify each repo is clean afterward, and finish with the root submodule-pointer + infra commit. Confidence: 0.85
- Include a `Co-authored-by: CommandCodeBot <noreply@commandcode.ai>` trailer on every git commit (passed as a second `-m` flag). Confidence: 0.9
- On this Windows/cmd environment, use `git commit -m "<subject>" -m "<body/trailer>"` flags instead of shell heredocs (`git commit -F - <<'EOF'`) — heredocs do not work in cmd and the commit fails. Confidence: 0.75
- curl is not installed on this Windows host — for HTTP endpoint/health checks against local services, use the PowerShell tool with `Invoke-WebRequest -UseBasicParsing` (or `Invoke-RestMethod`) instead of curl. Confidence: 0.80
- When the user gives a high-level instruction like "make it non-technical" or "strip the tech language" for end-user documentation, perform a full rewrite that removes all API calls, route paths, code tags, and technical status identifiers — replacing them with plain-English descriptions, role-oriented terminology, and conversational tone throughout. Confidence: 0.85
- When redesigning end-user reference/operations documentation for non-technical audiences, simplify system status names to human-readable labels (e.g., `PendingAgentApproval` → "Awaiting Agent", `DeniedByAgent` → "Denied by Agent") everywhere — in status reference tables, state flow diagrams, and FAQ answers. Never expose internal enum names to non-technical readers. Confidence: 0.80
- Always follow established project workflows, including creating phase verification files when implementing plan phases. Confidence: 0.85
- When a UI bug category is reported (e.g., "unnecessary scroll"), first conduct a comprehensive codebase-wide audit — launch parallel subagents to independently examine layout shells, component libraries, and CSS patterns simultaneously — then present findings in a structured report with severity levels, root causes, and fix directions, and let the user confirm before implementing fixes. Confidence: 0.80
- When the user provides a reference image and says "make it look like this" or "redesign to match this", treat the image as the primary design specification and implement a comprehensive redesign of the referenced page to match the visual layout shown — do not ask for written specs or step-by-step approval on design elements. The image is the spec. Confidence: 0.80
- When the assistant creates a detailed, categorized plan (brainstorm document with named sections) and the user selects sections by name (e.g., "Modern checkout UX", "Trust signals", "11 quick wins"), implement ALL items in those sections in one pass across all relevant layers (frontend + backend) without asking for further step-by-step approval on individual items. The user's selection of a section by name signals full endorsement of everything in it. Confidence: 0.85
- Treat "quick wins" (low-effort, high-impact improvements) as a first-class, implementable work category alongside conceptual feature sections — when the user asks for quick wins, implement them immediately as part of the same pass, not deferred to a follow-up. Confidence: 0.80
- When production code changes (new constructor parameters, changed DTO/record signatures) break the test project, fix the tests immediately by adding matching fake repository/service implementations and updating all constructor/DTO calls — do not leave tests in a broken state. The test project should build and pass before the task is considered done. Confidence: 0.85
- When adding a new page/route **or extending an existing page's frontend role access**, update all gate layers together: sidebar navigation (label, icon, href, roles), proxy/middleware route roles, and the backend controller authorization policy — keeping any one of them out of sync creates either broken navigation or authorization failures. The backend API authorization is a separate gate that must be independently verified and updated; do not assume it already permits the new role just because the frontend nav does. Confidence: 0.9
- When broadening a backend controller authorization policy to allow additional roles, also audit the **service layer** for hardcoded role-specific assumptions (e.g., looking up the user via a single role's repository, extracting `GameRoomId` from a single-role entity, expecting only one caller type in conditional logic). The service layer will compile fine but silently malfunction for the newly-allowed role. Extend DTOs with optional parameters to let the new role provide data the old role fetched implicitly. Confidence: 0.85
- When aligning implementation to an external API spec (PDF, official docs), start with an audit phase: extract the spec, do a complete field-by-field comparison against the implementation, document all gaps before writing any code. Confidence: 0.85
- Create a detailed build plan (scope, architecture, phases, open decisions) before implementing; execute sequentially and confirm each phase before proceeding to the next. Confidence: 0.85
- Before finalizing an implementation plan, surface decisions that materially change the shape of the work as structured multiple-choice questions with a recommended option (e.g., file format, data model, CRUD UI scope, auth scope) — the user expects to lock these in upfront, not revisit them mid-implementation. Confidence: 0.75
- When using plan mode, output the plan as readable text in the assistant message — do not rely on tool outputs or plan-mode-only visibility that the user cannot see. Confidence: 0.80
- Explore existing templates and patterns in the codebase before writing new code — always check what already exists in similar projects first. Confidence: 0.80
- When an API spec or behavior is ambiguous, make it configurable via env var rather than hardcoding a guess. Confidence: 0.75
- Use todo tracking (`todo_write`) to break implementation into sequenced phases, updating status as each phase completes — this keeps progress visible and organized during multi-file changes. Confidence: 0.8
- Always apply database migrations automatically as part of build/deploy — never ask the user to run migrations manually. If the app startup already calls `MigrateAsync()`, generate the missing migration and rebuild; if it doesn't, add auto-migration to the startup pipeline first. Confidence: 0.85
- When database changes are involved, always include an explicit "Run migrations" step in the implementation plan itself — do not assume migrations will be handled implicitly by the build or deploy steps. Confidence: 0.85
- When extending sidebar/feature access to an additional user role (e.g., adding "employee" to a feature previously restricted to "agent"), also gate create/edit/modify/delete actions behind the original restricted roles so the newly-added role sees a read-only view with only copy/share capabilities. Extending nav access without protecting inline actions gives the new role unintended write access. Confidence: 0.80
- When the user gives a terse fix request ("fix - in [feature/dialog name], i need to see [desired outcome]" or "fix bug -[Feature] [problem description]"), treat it as an autonomous task: explore the codebase to locate the referenced feature, diagnose the root cause, implement the fix, and verify it builds — do not ask clarifying questions about implementation approach. The user trusts the assistant to find the right solution independently. Confidence: 0.85


- When a bug or formatting issue is reported in one page/component, proactively grep for the same pattern in all sibling/related pages (e.g., other dashboard variants, detail dialogs for similar entities) and apply the same fix across them — don't fix only the reported page. The user expects visual consistency and will consider partial fixes as incomplete. Confidence: 0.85
- After deploying/rebuilding Docker services, proactively verify that EF Core migrations were actually applied in the running database (check `__EFMigrationsHistory`, query for new tables, or test new endpoints) and report the result to the user — do not wait for the user to ask "are the migrations applied?" Confidence: 0.80
- When rebuilding a Postman collection from scratch (not just updating existing items), do a complete audit by reading **every controller file in full** — not just grepping route attributes. Reading the full source reveals request/response DTOs, auth attributes, route parameters, and endpoint semantics that a simple route grep misses, ensuring the collection faithfully represents the entire API surface. Confidence: 0.80
- When scaffolding a new project, replicate the full agent/instruction/taste structure from an existing workspace, not just application code: workspace-root orchestration layer (top-level `AGENTS.md`, `.commandcode/settings.json`, coordinator agent, custom slash commands, taste seeds), per-repo `.commandcode/AGENTS.md` as the authoritative architecture doc, dual-format agents in both `.commandcode/agents/` and `.github/agents/` (plus `.github/copilot-instructions.md` mirrors), and per-repo taste files. Confidence: 0.80
- When porting an existing workspace's structure to a new project, strip domain-specific content (e.g., payment-specific references like wallets, payouts, game rooms) down to placeholders while keeping the stack and conventions; seed only generic stack-wide taste rules (Docker workflow, React Query invalidation, no MSW mocks, error handling, EF migrations, API conventions) and drop domain-specific learnings. Confidence: 0.80
- New project scaffolds should be instructions/agents only (AGENTS.md files, agents, skills, commands, settings, `.github` mirrors, taste seeds) — do not scaffold application/solution skeletons unless the user explicitly asks for them. Confidence: 0.75
- New projects should live in a separate sibling workspace root (e.g., `D:\Projects\UTS\repos` containing `uts-api/` and `uts-ui/` subfolders) rather than inside an existing project's repos folder; name backend repos `{project}-api` and frontend repos `{project}-ui`. Confidence: 0.70
- When a plan or fix approach has been presented and the user replies with a bare go-ahead ("fix it", "start now", "resume now"), treat it as approval of the entire plan: begin execution immediately and work through all planned passes autonomously — do not re-present the plan, ask which subset to do, or pause for per-pass confirmation. Confidence: 0.80
- For substantial .NET backend work in this workspace (architecture reviews, layering refactors, multi-file fixes), delegate to the specialized `dotnet-platform-architect` subagent defined in `.commandcode/agents/` rather than editing directly. Prompts should be empirical and constraint-laden: point to the agent standard file + repo AGENTS.md, state verified current state with exact file paths and line numbers ("trust this, but read files before editing"), number the fixes, forbid scope creep and git commits, require following existing code style, and demand a green `dotnet build` before the agent returns. Confidence: 0.70
- For substantial frontend React/Next.js/UI architecture work (layout/component refactors, plan design), the plan MUST be reviewed by the specialized `react-ui-platform-architect` subagent defined in `.commandcode/agents/` before proceeding — the user explicitly stated this is always required and that plan review/approval is that agent's responsibility ("Yes and always remember, it's his responsibility"). Do not finalize or present architecture-relevant frontend plans from direct file exploration alone; hand them to the architect agent first and incorporate its feedback before implementing. Confidence: 0.9
- When finalizing an implementation plan in plan mode, persist it to a markdown file under `.commandcode/plans/` (e.g., `sponsor-onboarding.md`) before exiting plan mode — it becomes the durable reference the user can review and the implementation is checked against it. Confidence: 0.6
- When a user asks *how* to do something in the UI and the capability doesn't exist yet, treat it as an autonomous capability-gap remediation (not a documentation answer): confirm the gap across backend + frontend, then implement it end-to-end in one pass — backend service method + controller endpoint + unit tests + frontend server action + frontend component changes + rebuild/redeploy — and verify with a live API cycle + DB check + UI payload inspection before declaring done. Do not answer "you can't" and leave the user waiting; ship the capability. Confidence: 0.9
 a live API cycle + DB check + UI payload inspection before declaring done. Do not answer "you can't" and leave the user waiting; ship the capability. Confidence: 0.82
- This Windows host has `NODE_ENV=production` set globally, which makes npm treat `--omit=dev` as the default and silently skip devDependencies (typescript, tailwindcss, eslint) on `npm install`/`npm ci` — adding a dependency can prune them and break `npx tsc`/`npm run build`. Reinstall with `set NODE_ENV=development&& npm ci` (or `npm install --include=dev`) scoped to the command only; do not change the user's global env. The UI Dockerfile already uses `npm ci --include=dev` in its build stage, so container builds are unaffected. Confidence: 0.75

